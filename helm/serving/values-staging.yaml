# helm/serving/values-staging.yaml

replicaCount: 2

image:
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 8000

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"

autoscaling:
  enabled: false

env:
  ENVIRONMENT: "staging"
